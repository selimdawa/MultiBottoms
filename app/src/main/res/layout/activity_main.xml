<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/fragment_selected"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:padding="10dp"
        android:text="@string/app_name"
        android:textSize="21sp"
        android:textStyle="bold" />

    <io.selimdawa.multibottoms.bubble.BubbleBottomNavigation
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:mbn_animationDuration="-1"
        app:mbn_animationMode="slide"
        app:mbn_hasAnimation="true" />

    <!--
    app:mbn_backgroundBottomColor="@color/white"
    app:mbn_circleColor="@color/white"
    app:mbn_countBackgroundColor="@color/orange"
    app:mbn_countTextColor="@color/white"
    app:mbn_defaultIconColor="@color/blue"
    -->

</RelativeLayout>